/*
 * RoleType Messages
 *
 * This contains all the text for the RoleType container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RoleType';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RoleType container!',
  },
});
