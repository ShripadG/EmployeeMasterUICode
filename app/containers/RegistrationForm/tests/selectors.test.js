// import { selectRegistrationFormDomain } from '../selectors';

describe('selectRegistrationFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
