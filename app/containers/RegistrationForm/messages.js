/*
 * RegistrationForm Messages
 *
 * This contains all the text for the RegistrationForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RegistrationForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegistrationForm container!',
  },
});
